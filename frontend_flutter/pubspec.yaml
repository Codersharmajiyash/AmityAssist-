name: uniassist
description: Workflow-centric student service frontend for UNIASSIST.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  dio: ^5.7.0
  go_router: ^14.6.2
  shared_preferences: ^2.3.0
  flutter_animate: ^4.5.0
  local_auth: ^3.0.2
  connectivity_plus: ^7.3.1
  sqflite: ^2.4.3
  path_provider: ^2.1.6
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
